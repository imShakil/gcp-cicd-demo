steps:
  - name: 'bash'
    script: |
      #!/bin/bash
      set -e
      set -x
      echo "Hello World"
    
  - name: 'bash'
    script: |
      docker --version
      docker compose version

options:
  logging: CLOUD_LOGGING_ONLY
